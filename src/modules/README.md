# Domain modules

Per `docs/ARCHITECTURE.md`, implement `missions`, `sources`, `fingerprints`, `generation`, `questions`, `verification`, `catalog`, `export`, and `audit` under this directory in Gate 2+.
