# AI Schemas

## Status

**NOT YET ANALYZED** — schemas must not be weakened for convenience (Implementer forbidden).

## Purpose

Structured JSON (or equivalent) for pipeline stages: extraction, fingerprint, candidates, solver output, verification findings, Denedio export payload.

## Sections

TODO:

- Extraction schema
- Fingerprint schema
- Candidate question schema
- Choice / distractor metadata schema
- Solver result schema
- Verifier findings schema
- Export / import DTO alignment (from `docs/DENEDIO_CONTRACT.md`)

## Validation policy

TODO: Strict validation at boundaries; reject partial or ambiguous structures.
